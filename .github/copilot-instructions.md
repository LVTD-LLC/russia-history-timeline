# Copilot Instructions

This is an Astro 7 static site. Follow `AGENTS.md` for commands and workflow.

- Keep bilingual content synchronized in `src/data/timeline.ts`.
- Use `npm run build` before proposing changes.
- Do not add a client framework for simple UI behavior.
- Keep CapRover deployment files at the repo root.
