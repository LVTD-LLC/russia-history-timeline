# CLAUDE.md

Claude Code should follow `AGENTS.md` as the canonical project instructions.

Extra Claude-specific note: prefer editing `src/data/timeline.ts` for content changes and `src/pages/index.astro` for presentation changes. Keep PRs small and include `npm run build` output in the PR test plan.
